<%= "#{t("borrow_mailer.update_borrow_email.hi")} #{@user.name}" %>
<%= "#{t(".your_borrow")} #{@borrow.book.title} #{t(".from")} #{@borrow.date_borrow} #{t(".is_expired")}" %>
<%= t ".we_look_forward" %>
