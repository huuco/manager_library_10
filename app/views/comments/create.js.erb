$("#comments-list").prepend("<%= escape_javascript(render partial: "comments/comment",
  locals: {comment: @comment}) %>");
$("#comment-form").html("<%= escape_javascript render("comments/comment_form") %>");
