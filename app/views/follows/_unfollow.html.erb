<%= book.follows.size %>
<%= link_to fa_icon("flag"), book_follow_path(book, current_user.follows.find_by(book_id: book.id)),
  method: :delete, remote: true, rel: "tooltip", title: t(".unfollow") %>
