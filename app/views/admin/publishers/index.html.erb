<% provide(:title, t(".manage_publishers")) %>
<nav aria-label="breadcrumb" class="col-md-12">
  <ol class="breadcrumb">
    <li class="breadcrumb-item"><%= link_to t("home"), admin_root_path %></li>
    <li class="breadcrumb-item active" aria-current="page"><%= t ".publishers" %></li>
  </ol>
</nav>
<h5 id="result" class="col-md-12"><%= "List have #{@publishers.total_count} publishers" %></h5>
<div class="col-md-5">
  <%= form_tag url_for, method: :get, remote: true do %>
    <div class="form-group">
      <div class="input-group">
        <%= text_field_tag :search, params[:search], class: "form-control" %>
        <%= submit_tag "Search", name: nil, class: "btn-primary" %>
      </div>
    </div>
  <% end %>
</div>
<div class="col-md-5"></div>
<div class="col-md-2">
  <%= link_to t(".create_publisher"), new_admin_publisher_path, class: "btn" %>
</div>
<div class="col-md-12">
  <table class="table table-bordered table-hover bg-primary">
    <thead>
      <tr class="text-center">
        <th><%= t(".stt") %></th>
        <th><%= t(".name") %></th>
        <th><%= t(".books") %></th>
        <th><%= t(".address") %></th>
        <th><%= t(".phone") %></th>
        <th><%= t(".info") %></th>
        <th></th>
      </tr>
    </thead>
    <tbody class="bg-white" id="data-table">
      <%= render partial: "table", locals: {publishers: @publishers} %>
    </tbody>
  </table>
  <div id="pagination">
    <%= paginate @publishers, remote: true, window: 2, theme: "twitter-bootstrap-4",
      pagination_class: "justify-content-center", id: "pagination" %>
  </div>
</div>
